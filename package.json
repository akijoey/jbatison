{
  "name": "jbatison",
  "version": "1.0.0",
  "description": "SQL mapper framework for Java configure with json.",
  "private": true,
  "scripts": {
    "format": "devkit format",
    "prepare": "devkit prepare",
    "release": "devkit release"
  },
  "devDependencies": {
    "@akijoey/devkit": "latest"
  }
}
